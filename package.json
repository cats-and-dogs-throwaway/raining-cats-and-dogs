{
  "name": "json-replacement-endpoint",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.21.0 <25"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "prestart": "npm run build",
    "start": "node --env-file-if-exists=.env dist/src/server.js",
    "pretest": "npm run build",
    "test": "node --test \"dist/test/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "fastify": "5.12.4"
  }
}
